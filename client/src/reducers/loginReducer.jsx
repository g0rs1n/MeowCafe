import { createContext } from "react";

export function isLoginReducer () {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}